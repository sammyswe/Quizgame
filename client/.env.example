# URL of the Treasure Trap realtime server.
# Leave empty in local dev — the Vite proxy forwards /socket.io to localhost:3001.
# For deployed setups, e.g. VITE_SERVER_URL=https://treasure-trap-server.onrender.com
VITE_SERVER_URL=
