# Architecture Decision Records

ADRs written by Matt Pocock agent skills (`/domain-modeling`, `/grill-with-docs`,
`/improve-codebase-architecture`) live here as numbered Markdown files.

Product direction that is already LOCKED/PILOT still lives primarily in
`docs/context/` and `obsidian/`. Prefer linking those over duplicating them here.
