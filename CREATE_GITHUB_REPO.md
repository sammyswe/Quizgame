# This repository

Target: https://github.com/sammyswe/IQ-Party

If the Cursor GitHub App cannot push, grant it access:

GitHub → Settings → Applications → Cursor → Repository access → include `IQ-Party`.

Then an agent can push the design bank to `main`.
