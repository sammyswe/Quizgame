---
description: Active engine and client/server authority boundary
alwaysApply: true
---

- **Primary forward architecture (owner LOCKED 2026-07-20): Path B — Higgsfield Game
  Generator** under `hf-treasure-trap/` (`logic.js` + canvas `index.html` + Higgsfield art).
  Mobile-first multiplayer; live deploy is the beauty milestone target.
- The root React + Phaser 3 app under `client/src/game/` is **legacy reference**, not the
  polish target. Do not invest Path A polish unless the owner explicitly reopens it.
- Phaser code in `experiments/loot-drop/` remains archived reference.
- On Path B, `logic.js` is authoritative inside the Higgsfield room kernel; the client sends
  intents and renders `viewFor` only. Soft timers use client `timeout` actions validated by
  server timestamps (no `setTimeout` in `logic.js`).
- Legacy root still uses `server/src/engine.ts` + `shared/` if touched; prefer Path B for new
  gameplay/visual work.
- Animation timelines must tolerate late/out-of-order state and reconstruct after reconnect.
- Canonical product vision: `hf-treasure-trap/design/ART_BIBLE.md` + `docs/context/HANDOFF.md`.
  Compact router: `docs/context/INDEX.md`.
